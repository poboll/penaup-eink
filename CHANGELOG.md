# Changelog

## Unreleased · 2026-08-24

### Added

- 新增微信读书屏保整合：支持周报/月报摘要、3.68 英寸 Pro（视觉 528 × 792 竖向、film 792 × 528 兼容协议）预览、PNG/JPG/.film 下载和已连接 Pro 发送。
- 新增微信小程序“读书屏保”实验室：四种阅读场景、三种六色显影方式、示例预览、PNG 保存和 Pro BLE 发送；Skill Key 仅走请求头且不写入本机。
- 新增 `POST /api/v1/integrations/weread/snapshot` 临时转发接口；Key 不落库、不进 URL、响应或日志，支持带中文单位的阅读统计归一化。
- 新增 `/device/` 浏览器设备工具：BLE 连接、型号/分辨率/电量/Wi-Fi 状态回读、网络配置、网络清除、重启和恢复出厂。
- 新增 BLE OTA 发布清单 schema、草稿清单生成脚本和固件升级边界文档。
- 新增响应式设备工具设计：纸白编辑式布局、48px 操作目标、键盘焦点、Reduced Motion 和状态留痕。
- 新增受限权限的 `penaup-backup.service` / `penaup-backup.timer`，并补齐 FastAPI 关系迁移 fixture，覆盖设备、模板、片单、设置和推送历史。

### Changed

- 继续收口 Web、设备工具与微信小程序的浅灰白草纸面：近白纸面、墨黑主按钮、细黑边、轻微压印阴影和鼠尾草绿激活态现在使用同一套几何与触控尺寸；小程序导航栏同步到 `#F8F8F4`，避免首屏闪回旧米黄色。
- 读书壁纸实验室继续以 Pro 3.68 英寸竖向纸面为唯一视觉主线：预览画布保持 `528 × 792`，写入设备时仍由 film-core 旋转为兼容的 `792 × 528` 协议；验证文档同步到 Node 24 的最新门禁计数。

- 修复读书壁纸实验室在切换工作台/创作方式后继承旧内部滚动位置的问题；桌面、平板和手机重新进入时都会从纸面起点开始，显影方式说明也统一标明六色基底最多形成 48 种色彩观感。
- 统一 `film-core`、Web 和微信小程序的三种显影说明，避免同一张 Pro 屏保在不同端出现不同的颜色能力描述。
- 修复微信读书壁纸生成后的 Pro 预览被普通画布缩放逻辑误旋转并偏移到右下角的问题；读书屏保现在保持 `528 × 792` 竖向视觉纸面，照片创作画布继续使用各机型自己的显示变换，写入时才转为 `792 × 528` film 协议。
- 修复读书实验室本地图标回退，补齐连接、显影、下载和保存按钮的图标，避免未加载图标字体时出现孤立圆点。
- 修复自定义跨月周报的时间标签与日阅读顺序；周报不再套用月报过滤条件。
- 微信读书 gateway 增加 HTTPS 主机白名单与凭据/查询串校验，默认只允许官方主机。

- 设备维护命令统一经过 `device_state_uncertain`；重新广播并成功回读前不显示成功。
- 固件升级入口校验产品、发布状态、型号、BLE 协议、文件名、长度、SHA-256 和 Ed25519 签名。
- `/docs/` 由 Node Runtime 提供只读静态文档入口，便于设备工具打开维护说明。
- 工作台在桌面、平板和手机端统一主要按钮栅格与字号；网络面板同步 `aria-expanded` / `aria-hidden`，768px 顶部状态栏不再横向溢出。
- 修复平板和桌面固定底栏遮住编辑区的问题：宽屏导航回到纸面内容流并保持可滚动，手机继续保留安全区固定底栏。
- Web 与小程序壁纸实验室统一偏白糙米纸背景、汇文明朝体/衬线层级和较大的触控按钮；更新跨端文档与验证矩阵，明确 DevTools AppID 权限仍是外部门禁。
- 类纸表面再收口为更浅的灰白草纸：使用 `#F8F8F4` 主背景、`#F2F2EE` 纸影、低对比斜向纤维、轻噪点和纸片式不对称圆角按钮；主按钮改为墨黑印章色，模板中心逐张显示 Pro 3.68 英寸竖向输出规格，主页同步展示约 258.7 PPI 与兼容 film 协议方向。
- 修复首页导航“打开工作台”墨黑按钮的文字对比度；默认状态使用白字，鼠标悬停与键盘聚焦时切换为鼠尾草绿纸面与墨字，避免按钮只剩一块黑色纸片。
- 工作台创作方式补齐键盘方向键、Home/End、焦点回收与 tabpanel 无障碍状态；页面导航同步 `aria-hidden`，键盘用户与 VoiceOver 不会落入已隐藏的创作页面。
- 设备工具新增 Web/Node 共用的严格固件清单校验，绑定当前机型、文件名、长度、版本、签名形状和 Ed25519 消息；OTA_LEN 已被接受后即使尚未发送数据块也进入状态待确认，并清理重连时重复的 BLE 通知监听。

### Safety

- 当前仓库没有正式发布 `.bin` 与固定发布公钥，刷写按钮默认锁定；ESP-IDF 构建目录产物不作为发布包。
- Web Serial 只做能力探测，不猜测 USB bootloader 协议或分区地址。
- 网页读书壁纸的 Skill Key 改为仅保留在当前页面内存，不再提供 `localStorage` 持久化选项；契约门禁会阻止这条隐私边界回退。
- 管理台图片资源收紧为本站 `/api/` 与 `/assets/` 路径，动态 HTML、模板图层字段和颜色参数统一转义/校验；拒绝路径穿越、外部 URL 和主动资源语法。
- `.film` 上传不再信任 multipart MIME，用户图片与管理台导入会清理图片元数据；设备心跳改用独立 IP 限流，避免普通 API 流量和心跳互相影响。
- 设备工具进一步限制 BLE OTA：必须匹配 Penaup Service UUID、固定签名 `key_id`、SHA-256、Ed25519 和 1536 KiB 上限；没有正式发布包时继续保持锁定。
- FastAPI 导入器拒绝越界媒体路径和源目录外符号链接，避免迁移阶段读取非旧库媒体；fixture 继续验证源库只读。
