# 微信旧云开发材料

这里仅保存接管前的 `quickstartFunctions` 示例和旧部署脚本，作为来源记录与回滚检索材料。

- 不属于 `apps/wechat/` 的 Penaup 小程序工程；
- 不属于 `server/` 的生产后端；
- 未经过权限、数据模型和依赖审计，不得部署；
- 若未来需要恢复，先建立独立的迁移说明和安全审查记录。

新产品展示名统一使用“花生片 Penaup”，历史 quickstart 内容不代表产品功能或版权归属。
