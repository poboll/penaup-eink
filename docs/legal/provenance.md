# 花生片 Penaup 权利与来源说明

> Copyright (c) 2026 poboll · 本文只记录已知来源与发布前复核边界。

## 许可范围

根目录 [LICENSE](../../LICENSE) 的 `Poboll Non-Commercial License v1.0` 只适用于本仓库中明确由 `poboll` 新增或重写、并且没有更早第三方权利冲突的材料。该许可证禁止商业产品、商业部署、收费服务、商业组织内部运营以及以商业优势或金钱报酬为主要目的的使用；它不是 OSI 认证的开源许可证。

文件头中的 `Copyright (c) 2026 poboll` 表示本次新写入或明确重写的部分，不自动覆盖同一文件中既有的历史实现、第三方片段或外包交付。许可证和版权标记不会反向证明权利已经转让。

## 历史外包交付与旧固件

本仓库接管前包含历史外包代码、旧版 `FrameFilm` 文案和可能来自外部依赖的实现。外包合同、交付验收、源代码著作权转让/许可条款尚未在本仓库中完成逐文件核对。在证据归档和法律复核完成前：

- 不把历史外包代码整体宣称为 `poboll` 独占原创；
- 不删除原始作者、第三方许可证、SDK 或数据手册的来源信息；
- 不因为目录、产品名或文件头变化而重新许可第三方代码和资产；
- 发布源码、固件、二进制或在线服务前，分别建立依赖和组合许可清单。

`firmware/penaup/` 是旧 `firmware/frame_film/` 的唯一迁移真源。其历史 C 文件仍可能保留 `kiritro` 和 `GPL-3.0-or-later` 的原始标记；根目录非商业许可证不会追溯性撤销 GPL，也不会把历史文件改成 `poboll` 独占材料。只有合同或权利转让证据明确覆盖对应文件后，才可另行决定许可标记。

`FrameFilm`、`frame_film` 和 `FRAMEFILM_*` 只作为旧设备名、旧目录、旧广播名、历史协议和迁移检索词保留。新的产品展示名统一使用 `花生片 Penaup`。

## 第三方与外部资产清单（发布前补全）

| 类别 | 当前处理 | 发布前动作 |
|---|---|---|
| ESP-IDF、ESP32 工具链 | 依照各自 SDK 许可 | 固件分发时附带对应许可与版本 |
| `kiritro / GPL-3.0-or-later` 历史固件 | 保留原始来源和许可边界 | 核对修改、链接和二进制分发义务 |
| EPD 厂商驱动、LUT、数据手册 | 硬件资料/供应商材料 | 确认是否允许再分发，必要时只发布链接或摘要 |
| 汇文明朝体 Web 子集 `apps/web/fonts/huiwen-mincho.woff2` | 由本机 `/Users/Apple/Downloads/Huiwenmingchaoti/汇文明朝体.otf` 生成，用于微信读书 Pro 屏保排版 | 发布前核对原字体授权、子集生成许可、版本和 SHA-256；未核实前不宣称为 poboll 独占资产 |
| `Sodamax778/expert-eureka` 流程参考 | 仅参考公开可见的阅读数据归一化、固定模板和本地导出思路；未复制代码或素材 | 发布前重新核对上游许可证；未取得明确授权前不将其代码或资产并入本仓库 |
| 系统字体、历史图标、图片和产品摄影 | 当前 Web 工作台不再依赖远程字体；历史资产来源仍需逐项核对 | 补充作者、URL、版本、许可证和文件 SHA-256 |
| 外包交付代码与设计稿 | 合同权利待核对 | 保存合同、验收记录和权利转让/许可凭证 |
| npm 依赖与 MQTT 客户端 | 依赖自身许可证 | 发布包前生成锁文件和依赖许可证报告 |

## 维护规则

新代码、文档、设计 token 和产品文案如由 `poboll` 创作，可标注：

```text
Copyright (c) 2026 poboll
SPDX-License-Identifier: LicenseRef-Poboll-NonCommercial
```

若文件混合历史代码和新修改，应保留历史来源，在变更点或文件头说明“本次修改由 `poboll` 完成”，不要抹平作者边界。发布检查表见 [release-checklist.md](release-checklist.md)。
